// Alias route: Partner Center webhook URL points to /api/webhooks/tiktok.
// Handler lives in /api/tiktok/webhook (signature verification + service client).
export { POST } from '../../tiktok/webhook/route'
